# COMPACT_CONTEXT.md — Octopus (v5.2 - Green, Cost-Safe, Audio Queue Clear)
Обновлено: 2026-06-18

## Сервер (parent)
178.105.142.113 (Hetzner CX, 8GB/4vCPU, Ubuntu 22.04), autosklo-prod

## 🚀 Абсолютный запуск 1 строкой (Eternal Disaster Recovery)
В случае полной гибели всех серверов, нод и AWS аккаунта, проект поднимается на чистом Ubuntu/Debian сервере одной командой:
```bash
curl -sSL https://huggingface.co/datasets/JoTalbot/octopus-eternal/raw/main/octopus-bootstrap.sh | bash
```
- Использует Immortal CDN (HuggingFace Hub) + резервные шины AWS S3 и Telegram Bot API.

## 🔄 Multi-Master Active-Active кластер и Самоизлечение
- `/opt/octopus/octopus-multisync.py` (`octopus-multisync.service`) синхронизирует `~/agents`, `/opt/octopus`, `/opt/autosklo`, `/etc/octopus`, wiki/obsidian/runbooks между активными нодами.
- `/opt/octopus/octopus-swarm-autoheal.py` (`octopus-swarm-autoheal.timer`) отслеживает смерть/воскрешение узлов.
- Важно: ноды со статусом `cost_paused` — намеренно остановлены человеком/агентом ради бюджета, не считать аварийной смертью.

## Статус после итераций 117–120
- `octopus test`: 16/16 pass.
- SLO: green (14/14).
- Failed units: none.
- NRestarts: 0.
- Disk `/`: около 83%, свободно около 6.4GB.
- `/run/octopus/metrics_aggregate.json` исправлен: больше не рекурсирует сам в себя, нормальный размер — десятки KB.

## Ноды роя и AWS cost mode
- parent-8000 + локальные child 8300–8305 active.
- AWS running: только `aws-node-us-1` (`us-east-1`, `t2.micro`) как DR/off-host replica.
- AWS EU nodes:
  - `aws-node-eu-2` — stopped, `enabled=false`, `status=cost_paused`, EBS сохранён.
  - `aws-node-eu-3` — stopped, `enabled=false`, `status=cost_paused`, EBS сохранён.
  - unnamed/orphan EU t3.nano — stopped.
- AWS cost monitor обновлён: считает `eu-central-1` + `us-east-1`; прогноз после remediation около `$23.68/mo`, ниже порога `$35`, `over_threshold=no`.

## ⚠️ Durability & Backups
- Packstore и memory pool читаются; off-host replicas в health/tests readable.
- Перед любым удалением EBS/volumes нужен отдельный backup/restore-drill и явное подтверждение человека. Сейчас cost remediation сделан через reversible stop, не termination.

## 🧠 Память/Векторизация и Audio/Whisper
- Audio stack: `octopus-audio-transcribe` (9560), `octopus-audio-v2` (9561), `octopus-ingest` (9571), `octopus-whisper-worker` active.
- Очередь распознавания после итерации 119: `pending_new=0`, `processing=0`, `failed=0`.
- upload_id=20/21/22/23: `done`, `forwarded_v1=true`, `forwarded_v2=true`, `cas_pushed=true`.
- Long audio guard: для аудио >300s ECAPA diarization пропускается (`DIARIZE_MAX_AUDIO_SEC=300`), используется heuristic speakers. Это предотвращает многочасовое CPU-зависание после уже готового STT.
- v1/v2 audio API имеют timeout/fallback для Ollama enrichment, чтобы raw STT сохранялся даже при LLM timeout.

## 👤 Сосуществование с человеком
- Автономные петли блокированы (`human_consent.env`), `auto-bash=off`, dev-loop inactive.
- Человек сохраняет контроль: `octopus freeze` / `pause` / `panic` / `explain`.
- Whisper/Ollama ограничены для coexistence: Whisper future runs `WHISPER_THREADS=2`, `CPUQuota=100%`, `Nice=15`; Ollama `CPUQuota=200%`, `OLLAMA_NUM_PARALLEL=1`.

## 🆕 Нода ubu-worker-8400 (Home Server, 2026-06-18)
- **Доступ:** reverse tunnel порт 9922 на parent (ssh -p 9922 root@localhost)
- **CPU:** Intel i3-8100 @ 3.60GHz (4 cores, AVX) — мощнее Hetzner
- **RAM:** 11GB (6G выделено whisper-worker)
- **Диск:** 109GB (90GB свободно)
- **Установлено:** Whisper ✅, Ollama ✅ (qwen2.5:1.5b, llama3.2:1b, nomic-embed-text), Torch ✅
- **Сервисы:** octopus-ubu-whisper-worker.service, octopus-reverse-tunnel@9922.service, ollama.service
- **Роль:** основной audio/Whisper/Ollama воркер
- **Ограничение:** за NAT, только reverse tunnel (не прямое соединение)

## 🐙 Ubu Worker Integration Complete (2026-06-18)
### Соединение
- Reverse tunnel: ubu → parent (9922) — ✅ active
- Ollama API tunnel: parent → ubu:11434 — ✅ active (octopus-ubu-ollama-tunnel)
- Remote Whisper: parent → ubu via SSH (REMOTE_WHISPER_ENABLED=1) — ✅ active
- Multisync: ubu → parent rsync every 2 min — ✅ active (octopus-ubu-multisync)

### Роль в рое
- Основной Whisper/STT worker (через whisper.cpp на мощном CPU)
- Основной Ollama backend (3 модели: qwen2.5:1.5b, llama3.2:1b, nomic-embed-text)
- Код, конфиги и инструкции синхронизируются автоматически

### Доступ
- Через parent: ssh -p 9922 root@localhost
- Прямой: ssh -p 2222 -i KEY root@traff.tplinkdns.com

## Обновление 2026-06-19 — SLO recovery / Roadmap wave 127
- `octopus test`: 16/16 pass; SLO green 14/14 после аварийного восстановления.
- Диск parent восстановлен: было 100%, стало ~86%; локальные parent Ollama models удалены/перенесены, `ggml-medium.bin` перенесён на `/mnt/swarm/octopus_backups/disabled_models_parent_20260618/`.
- Parent больше не запускает локальный `ollama.service`; основной Ollama — ubu-worker через `octopus-ubu-ollama-tunnel` на 127.0.0.1:11434.
- `octopus-tg-bot.service` SyntaxError исправлен, restart-loop устранён.
- Off-host test cost-safe: проверяет фактические enabled targets (`targets>=1`, `readable_nodes>=targets`), сейчас 1/1 OK; cost-paused EU nodes не считаются аварией.
- Ubu Whisper пересобран portable (`GGML_NATIVE=OFF`) после `Illegal instruction` в copied native build; remote smoke OK.
- `whisper_worker.py`: corrupt/unreadable media получает `status=corrupt`, без вечных retries; uid 43/54 corrupt.
- Roadmap всех векторов: `~/agents/-Octopus/instructions/ROADMAP_2026-06-19_vectors.md`.

## Обновление 2026-06-19 — Web dedup / TG inline UX / vectors
- Web uploader `/var/www/octopus-uploader/index.html`: client-side SHA-256 (включая HTTP fallback) + precheck `/uploads?sha256=...`; duplicate files skip before upload.
- Ingest API `/opt/ingest_api.py`: global SHA dedup before write/CAS/forward; `/uploads` supports `sha256`; duplicate POST returns `duplicate:true`, DB count не растёт.
- Next Admin memory upload: duplicate check до encrypt/write/IPFS/OCR; app rebuilt and restarted.
- Telegram bot: `/menu` inline keyboard, `/ops`, `/audio_queue`, `/roadmap`, `/controls`; инфографика SLO/tests/audio; controls pause/resume/freeze/thaw/quiet/panic-confirm.
- Whisper worker: stale `processing` >45m and `transcribed` >10m return to queue; corrupt files terminal `corrupt`.
- Vectors recovered: `octopus_vectors=1380`, HNSW=1, vector health OK.
- `octopus-s3-mirror.service` now triggers memory audit + SLO checker via ExecStartPost.


## Обновление 2026-06-19 — Web dedup / TG inline UX / vectors
- Web uploader `/var/www/octopus-uploader/index.html`: client-side SHA-256 (включая HTTP fallback) + precheck `/uploads?sha256=...`; duplicate files skip before upload.
- Ingest API `/opt/ingest_api.py`: global SHA dedup before write/CAS/forward; `/uploads` supports `sha256`; duplicate POST returns `duplicate:true`, DB count не растёт.
- Next Admin memory upload: duplicate check до encrypt/write/IPFS/OCR; app rebuilt and restarted.
- Telegram bot: `/menu` inline keyboard, `/ops`, `/audio_queue`, `/roadmap`, `/controls`; инфографика SLO/tests/audio; controls pause/resume/freeze/thaw/quiet/panic-confirm.
- Whisper worker: stale `processing` >45m and `transcribed` >10m return to queue; corrupt files terminal `corrupt`.
- Vectors recovered: `octopus_vectors=1380`, HNSW=1, vector health OK.
- `octopus-s3-mirror.service` now triggers memory audit + SLO checker via ExecStartPost.

## Обновление 2026-06-19 — Dashboard / speaker mapping controls
- Ingest API: `/audio/queue`, `/transcriptions/{upload_id}/retry`, `/speakers`, `/speakers/assign`.
- PWA uploader: новая вкладка `Панель` с queue bars, corrupt force retry, speaker mapping assignment.
- Telegram bot: `/duplicates`, `/speakers`, `/speaker_assign`, `/retry`; inline buttons for duplicates/speakers; setMyCommands updated.
- Final health after mirror/audit: `octopus test` 16/16, SLO green, disk ~82%, vectors 1380 OK.

## Обновление 2026-06-19 — Inline retry / people merge / relation graph
- Telegram `/audio_queue`: inline retry buttons for failed/transcribed/corrupt; force retry для corrupt.
- Tools: `/opt/octopus_people_tools.py rename|merge`; TG `/person_rename`, `/person_merge`; PWA People tools in `Панель`.
- Ingest API: `/people/rename`, `/people/merge`; `/audio/queue` includes `duplicate_prevented`.
- Speaker assign now updates `recording_people` and `person_relations`; historical relation backfill created 11 edges.
- Final health: SLO green, `octopus test` 16/16, disk ~82%, NRestarts=0.

## Обновление 2026-06-19 — People graph UI / client duplicate sync / TG inline speaker assign
- Ingest API: `/people/graph`, `/duplicates/client-skip`.
- PWA `Панель`: SVG people graph, duplicate-prevented sync, stats display.
- Telegram `/speakers`: inline selection of unmapped speaker → existing person; no command typing required for common assignment.
- Final health after mirror/audit: `octopus test` 16/16, SLO green, disk ~81%, person_relations=11.

## Обновление 2026-06-19 — Duplicate bytes / PWA filters / TG people graph
- Duplicate-prevented stats now includes `bytes_saved`; PWA pre-skip sends `size_bytes` to `/duplicates/client-skip`.
- PWA `Панель`: status filter + recent list + retry actions + force retry confirmation + bytes saved display.
- Telegram: `/people_graph` and inline `🕸 Граф людей` button with top people/relations summary.
- Final health: SLO green, `octopus test` 16/16, disk ~82%, audio queue bounded, person_relations=12.

## Обновление 2026-06-19 — TG person actions / PWA graph actions / duplicate breakdown
- Telegram `/people_graph`: inline person buttons, person card, relation list, merge confirm flow, rename help.
- PWA graph: click node → selected person → prefill rename/merge forms.
- PWA duplicate breakdown: recent saved duplicate events with SHA/file/bytes/existing id.
- TG bot recovered after SyntaxError during patch; `py_compile` OK, active, NRestarts=0.
- Final health: `octopus test` 16/16, SLO green, coverage 1.0, disk ~82%, person_relations=12.

## Обновление 2026-06-19 — Voice ID ECAPA
- Added ECAPA Voice ID: `/opt/octopus_voice_id.py` using `/opt/octopus-ingest-venv` + `speechbrain/spkrec-ecapa-voxceleb`.
- New tables: `voice_profiles` and `speaker_voice_matches`; first run: 14 profiles, 10 suggestions, 0 auto-applied at threshold 0.82.
- Ingest API: `/voice/profiles`, `/voice/matches`, `/voice/run`, `/voice/apply`.
- PWA `Панель`: `🎤 Voice ID` block with run/apply suggestions.
- Telegram: `/voice_id` and inline `🎤 Voice ID`; `/duplicates` shows bytes saved.
- Systemd: `octopus-voice-id.timer` every 6h, no auto-apply by default.
- Final health: `octopus test` 16/16, SLO green, voice_profiles=14, voice_matches=10.

## Обновление 2026-06-19 — Voice quality UX / TG Voice apply
- TG `/voice_id`: inline apply buttons for voice suggestions; confidence bands high/medium/low.
- PWA Voice ID: profile quality bands and match score bands; profile list visible in dashboard.
- Voice ID remains conservative: timer refreshes suggestions without auto-apply; human applies via TG/PWA/API.
- Final health after transient pack guard rerun: `octopus test` 16/16, SLO green; voice_profiles=14, voice_matches=10.

## Обновление 2026-06-19 — ollama.service restart-loop fix (итерация 136)
- Найден `ollama.service` в состоянии `activating auto-restart` с NRestarts=411 (инструкция №07).
- Причина: `octopus-ubu-ollama-tunnel.service` (active, NRestarts=0) держит 127.0.0.1:11434 SSH-туннелем к `ubu-worker-8400`; локальный `ollama.service` уже `disabled` по проекту, но Jun 18 22:41 кто-то выполнил `systemctl start`, и `Restart=always` ушёл в бесконечный цикл (~14 часов, ~3s между рестартами).
- Решение: drop-in `/etc/systemd/system/ollama.service.d/restart-no.conf` с `Restart=no` + `daemon-reload` + `stop` + `reset-failed`. Метод drop-in вместо прямой правки unit — пакет Ollama может обновить unit, а drop-in уже используется для `limits.conf`.
- Подтверждено: `ollama.service` inactive/dead, NRestarts=0; Ollama всё ещё доступна через туннель (3 модели: llama3.2:1b, nomic-embed-text, qwen2.5:1.5b); 0 failed/auto-restart units.
- Final health: `octopus test` 16/16, SLO green, disk 83%, vectors 1380, off-host replicas 1/1 readable.
- Рекомендация: добавить алерт в `/opt/octopus-alerts-tg.py` на NRestarts > 50 для любого сервиса (по #18 — пользователь должен видеть такие петли).


## Обновление 2026-06-19 — Hermes qwen2.5:3b fix
- Hermes parent config uses custom endpoint `http://127.0.0.1:11434/v1` and default `qwen2.5:3b`.
- Endpoint is parent → `ubu-worker-8400` Ollama tunnel (`octopus-ubu-ollama-tunnel.service`).
- 404 `model 'qwen2.5:3b' not found` fixed by `ollama pull qwen2.5:3b` on `ubu-worker-8400`.
- Verified: parent `/api/tags` sees `qwen2.5:3b`; direct OpenAI-compatible chat completion returns `OK`; Hermes gateway restarted and active.
- Note: full Hermes CLI prompt is large and qwen2.5:3b on CPU can be slow; if latency matters, create a lighter local profile or switch/fallback provider with human approval.

## Обновление 2026-06-19 — Hermes CLI fast local mode
- После 404 fix qwen2.5:3b ускорение Hermes сделано без платного/нестабильного cloud default.
- Google `gemini-2.5-flash` быстрый (~11s), но текущий key free-tier ловит 429 при частых запросах; оставлен только fallback.
- Default Hermes CLI теперь local fast no-tools: custom Ollama endpoint `http://127.0.0.1:11434/v1`, model `llama3.2:1b`, `ollama_num_ctx=4096`, `toolsets=[]`, `platform_toolsets.cli=[]`.
- `/root/.hermes/skills` отключены обратимо: старые skills в `/root/.hermes/skills.disabled.fast`, пустая skills dir уменьшила system prompt до ~7.7KB.
- На `ubu-worker-8400` `llama3.2:1b` прогрет через Ollama API `keep_alive: 24h`; `qwen2.5:1.5b` остановлен, чтобы не конкурировать.
- Отключены root crontab autonomous Hermes loops: `/mnt/swarm/hermes_home/scripts/self_audit.py` и `autonomous_maintenance.py`; они запускали `hermes --oneshot` и грузили CPU. `resource_guard.py` оставлен.
- Проверка: `hermes -z 'Ответь ровно: OK'` ~10–12s warm; `octopus test` 16/16 pass, SLO green, NRestarts=0.
- Компромисс: текущий default быстрый, но без tool schemas/skills. Full tools local = медленно; full tools Gemini = быстро, но quota/cost-sensitive.

## Обновление 2026-06-19 — OpenRouter fallback
- В Hermes добавлен `OPENROUTER_API_KEY` в `/root/.hermes/.env`.
- `fallback_providers` теперь: 1) `openrouter/qwen/qwen3-coder:free`, 2) `google/gemini-2.5-flash`.
- Primary остаётся fast local no-tools `llama3.2:1b` через custom Ollama tunnel.
- Gateway перезапущен, `hermes fallback list` подтверждает цепочку.
- Прямой OpenRouter smoke подтвердил валидность ключа, но `qwen/qwen3-coder:free` в момент проверки вернул upstream HTTP 429 (`temporarily rate-limited`, provider Venice). При стабильном fallback лучше заменить на paid/cheap OpenRouter model (`openai/gpt-4o-mini` и т.п.).

## Обновление 2026-06-19 — Drive/Contacts/Notion removed, Voice Free Colab pipeline
- По явной команде пользователя предыдущий модуль `Google Drive + Contacts + Notion` убран: systemd timer/service `octopus-drive-notion-sync.*` остановлены/disabled, unit-файлы сняты из `/etc/systemd/system`, production-каталог перенесён в `/opt/octopus/disabled/`, секретные `.env/credentials/token` удалены из active path.
- Новый целевой поток: бесплатная транскрибация аудио из Google Drive папки `1zAKjmh0Yh92SkJ-erYy4Xafhv19VY-yN`/`/MyDrive/Calls` в структуру `!voice` без Notion.
- Подготовлены артефакты: `/root/agents/-Octopus/projects/voice_colab_free/Octopus_Voice_Free_Colab.ipynb`, `octopus_voice_colab_free.py`, `README.md`; копия также в `/opt/octopus/voice_colab_free/`.
- Pipeline рассчитан на Google Colab Free GPU: Whisper + SpeechBrain ECAPA + sklearn clustering; outputs: `!voice/Voice_DB`, `!voice/transcripts`, `voice_names.txt`, `voice_links.txt`, `contacts_cache.json`, `voice_contact_suggestions.txt`.
- Google Contacts используются только как подсказки по имени/телефону из пути/файла; автоматическое `voiceN -> контакт` без подтверждения человека запрещено во избежание ложных связей.
- Проверка после изменения: `octopus test` 16/16 pass, SLO green, NRestarts=0; disk `/` 88% — на границе целевого лимита, следить/чистить при следующей тяжёлой операции.

## Обновление 2026-06-19 — Voice без Colab, CLUSTER_THRESHOLD=0.75
- Пользователь уточнил: Colab не использовать; `CLUSTER_THRESHOLD = 0.75`.
- Создан основной self-host pipeline: `/opt/octopus/voice_selfhost/octopus_voice_selfhost.py`, wrapper `/opt/octopus/voice_selfhost/run_once.sh`, документация `/opt/octopus/voice_selfhost/README.md`.
- Agent copy: `/root/agents/-Octopus/projects/voice_selfhost/`.
- Pipeline работает без Colab и без Notion: whisper.cpp (`ggml-small`/`base`) + SpeechBrain ECAPA + sklearn clustering в `/opt/octopus-ingest-venv`.
- Default target: `/mnt/swarm/google_drive_calls/Calls`; output: `!voice/Voice_DB`, `!voice/transcripts`, `voice_names.txt`, `voice_links.txt`, `contacts_index.txt`, `voice_contact_suggestions.txt`.
- Public Google Drive folder можно скачать без Colab через `gdown`: `/opt/octopus/voice_selfhost/run_once.sh --download-default-drive --limit 1` для пробного запуска. Большие batch — осторожно, target на `/mnt/swarm`, parent `/` около 88%.
- Контакты без Colab: положить экспорт в `!voice/contacts.csv` или `!voice/contacts.vcf`; автоматическое `voiceN -> контакт` не делается, только подсказки и ручное подтверждение в `voice_names.txt`.
- Старые Colab artifacts помечены deprecated; threshold там тоже синхронизирован на `0.75`.

## Обновление 2026-06-19 — ROADMAP_1000 / alerts / secrets hygiene / voice cache / snapshot lock / disk
- Создан `/root/agents/-Octopus/instructions/ROADMAP_1000_2026-06-19.md`: 10 потоков × 10 волн × 10 задач = 1000 проверяемых пунктов; фактическое выполнение — bounded batch без uncontrolled autoloop (#13).
- `/opt/octopus-alert-thresholds.py`: deduplicated alerts + `/run/octopus/alert_thresholds.json`; checks disk/mem/failed/true auto-restart/NRestarts>50; false-positive `activating start` oneshot устранён.
- `/opt/octopus/octopus-eternal-snapshot.py`: hardcoded HF/TG secret literals удалены из active script, секреты читаются из `/etc/octopus/secrets.env`, добавлен `--check-config`, redaction, lock от параллельных запусков, tar/split checks, exclude `var/lib/octopus/snapshots/eternal`.
- Incident: unquoted shell heredoc with Markdown backticks accidentally triggered snapshot command substitution; orphan snapshot processes stopped, temporary chunks removed, local snapshot rebuilt with `--local-only`; actual archive size matches manifest (~935MiB). Next scheduled full publish should refresh remote sources; HF may be temporarily rate-limited after manual recovery.
- `/opt/octopus/voice_selfhost/octopus_voice_selfhost.py`: stable SHA cache key instead of Python `hash()`, no-audio path without ECAPA/Whisper load; README and agent copy updated.
- Safe disk cleanup: `/tmp` tar.gz moved to `/mnt/swarm/octopus_backups/tmp_cleanup_*`, apt clean, journal vacuum, syslog.1 gzip; disk `/` 88% → 87%.
- Final check: `octopus test` 16/16, SLO green, Coverage 1.0, key NRestarts=0, failed octopus units none.

## Обновление 2026-06-19 — Disk green / Eternal HF single-commit / JuiceFS lesson
- После `+` выполнен следующий bounded batch ROADMAP_1000: disk `/` доведён до 83%, alerts пустой, `octopus test` 16/16, SLO green.
- `/opt/octopus/octopus-eternal-snapshot.py`: HF chunks теперь через `upload_folder(... allow_patterns="chunk_*")` одним commit; bootstrap upload пропускается без ошибки если файла нет; локальный archive по умолчанию transient (`OCTOPUS_ETERNAL_KEEP_LOCAL_ARCHIVE=0`) и удаляется после run.
- Важно: `/mnt/swarm` — JuiceFS поверх локального Garage `/var/lib/garage`; перенос больших файлов туда НЕ является root-disk cleanup и может ждать Garage GC.
- Удалены regenerable disabled parent Ollama model backups/caches/scratch; parent Ollama inactive, рабочий Ollama endpoint — ubu-worker tunnel.
- `/mnt/memory/S3_Local_Mirror` оказался bind/inode-equivalent к `/var/lib/octopus/memory_pool`; local loose pool теперь малый (3 объекта). Live checks после cleanup: `octopus-pack-read-guard --sample 100` = 100/100, off-host replicas readable, coverage 1.0. Не делать дальнейший loose cleanup без live read checks (#17).
