# Octopus — Рабочий план на 2026-07-26

## Текущее состояние

- **Навыки**: 284 total, 284 real, 0 stubs (включая дубликаты)
- **Дубликаты имён**: 7 (cas-credential-boundary-guard, docker-child-capacity, free-tier-preflight, node-reputation-reader, skill-factory, skill-health-monitor, skill-notification)
- **Векторы**: 8 (self_sustain, memory, live, simplify, coexist, reproduce, develop, learn, adapt)
- **Адаптация**: adapt=450/red (telegram_guard_ok=false, public_health_endpoint_failure, aws_ok=false)
- **Последняя выполненная волна**: Wave 8 (CI/QA Release Safety Manifest + nginx canonicalization)
- **Блокер**: Cloudflare named tunnel credential rotation (ручной, требует аккаунтной ротации)

## Фаза 1: Снятие блокеров и стабилизация (приоритет #1)

### 1.1 Ротация Cloudflare named tunnel credential
- **Статус**: ручной блокер из Wave 8
- **Действие**: ротация credential в Cloudflare account
- **Верификация**: проверка что старый credential больше не работает, новый работает
- **Зависимость**: нет, требуется только доступ к Cloudflare account

### 1.2 Merge дубликатов навыков (7 пар)
- **Приоритет**: medium
- **Действие**: для каждой пары дубликатов сравнить code/tests/references, объединить уникальный контент в canonical candidate, архивировать дубликат после валидации
- **Пары**:
  1. `core/cas-credential-boundary-guard` + `memory/cas-credential-boundary-guard`
  2. `core/docker-child-capacity` + `swarm/docker-child-capacity`
  3. `core/free-tier-preflight` + `swarm/free-tier-preflight`
  4. `core/node-reputation-reader` + `swarm/node-reputation-reader`
  5. `core/skill-factory` + `meta/skill-factory`
  6. `core/skill-health-monitor` + `meta/skill-health-monitor`
  7. `core/skill-notification` + `meta/skill-notification`
- **Верификация**: skills index обновлён, дубликатов 0, все тесты проходят

### 1.3 Обработка bounded adaptation proposals
- **Статус**: 1 proposal ready (merge_duplicate_skill-marketplace-sync), 1 ready (external_publication_reconcile), остальные пустые
- **Действие**: выполнить merge duplicate skill-marketplace-sync, закрыть дубликат issue #14

## Фаза 2: Безопасные next-waves (приоритет #2)

### 2.1 WAVE_PLAN_100 — Wave 2 (задачи 11-20)
- backup-gap-analyzer → real backup schedule coverage logic
- docker-child-capacity → container resource audit
- tunnel-health-audit → SSH/CF tunnel check
- node-reputation-reader → swarm reputation reader
- free-tier-preflight → free-tier resource check
- railway-health-reader → Railway API health
- github-actions-health-reader → GH Actions status
- log-retention-audit → log rotation policy check
- service-dependency-map → systemd dependency graph
- traffic-analyzer → nginx access-log patterns

### 2.2 WAVE_PLAN_SHAREDINTEGRATIONS — Iteration 2 (Wave 2)
- B5: Telegram OAuth живой (telegram_mcp) + webhook-верификация
- B6: OLX Partner API через integrations/olx/rules.json
- B7: Real OAuth provider; vault credential refs
- B8: Service Domain graph extension → prod
- B9: Supplier Mesh ≥3 вариантов исполнения

### 2.3 Vector Adapt (H) — добавить drift guards
- Новый drift guard по disk/skills/health
- Автоматическое создание bounded proposals
- Telegram guard восстановление

## Фаза 3: P1 foundation (приоритет #3)

### 3.1 Vector D — Memory / CAS / Packstore
- CAS manifest schema: hash, backend, timestamp, source, live-read status
- Packstore read-guard after dedupe/GC
- Memory proof endpoint: item -> hash -> backend -> citation chain
- Backup verify workflow with sample restores

### 3.2 Vector E — GraphRAG / Semantic Intelligence
- Graph schema: services, tasks, incidents, decisions, people, artifacts
- Ingestion from reports/logs/experience/docs
- Query modes: fast, deep, incident-review, decision-trace
- Citations to exact files/hashes
- Scheduled GraphRAG maintenance/report

### 3.3 Vector H — MCP / Dynamic Tools / Skills
- MCP read-only ops: status, report, registry read, logs tail, cleanup proposal
- Write tools require approval token and audit reason
- Command whitelist and denylist
- Tool-call tracing with trace_id
- Skills marketplace/index with tests

### 3.4 Vector I — Observability / Metrics / Traces
- trace_id in cron/timers/scripts/workflows
- Dashboards: disk forecast, restart budget, workflows, queue lag
- Alert dedupe and known-degraded suppression
- Daily incident digest

## Фаза 4: P2 и продуктовая ценность (приоритет #4)

### 4.1 Vector F — Audio / VoxRAG / Calls
- Idempotent audio job queue with retries
- Transcript + segments + speaker map in CAS/GraphRAG
- Manual correction UX for speakers/tasks/reminders
- Audio embeddings and cross-modal links
- Privacy gate and retention policy

### 4.2 Vector G — Swarm / Task Orchestration / Consensus
- Task lifecycle: proposed -> approved -> running -> verified -> archived
- Task reaper policies and dead-letter queue
- Local BFT/Raft pilot for metadata decisions only
- Quorum for destructive swarm actions
- Node reputation as signal, not authority

### 4.3 Vector L — Product / Business / AutoSklo
- AutoSklo workflows: lead intake, part search, quote, CRM notes
- Human approval for customer-facing messages
- Product dashboard: active leads, tasks, SLA, conversion
- Knowledge base from calls/transcripts/docs
- Weekly business report

### 4.4 Vector M — Free-node Expansion / Edge Compute (gated)
- Inventory of existing nodes and free-tier candidates
- Per-provider risk/cost/card requirement matrix
- Bootstrap script for read-only worker node
- No external creation without explicit gate

## Фаза 5: Финальная валидация (приоритет #5)

### 5.1 WAVE_PLAN_100 — Waves 3-10 (задачи 21-100)
- SharedIntegrations cleanup (delete mystic stubs)
- Write tests for telegram.py, social.py, google_business.py, common.py
- AutoSklo improvements (SSL, SEO, sitemap, schema.org, HSTS, mobile, GSC, speed, structured data)
- AutoHelp improvements (webhook testing, lead pipeline, OLX draft, connector config, secrets audit, Dockerfile, docker-compose, API status, test coverage)
- Infrastructure audits (systemd timers, disk, backup integrity, SSH config, firewall, fail2ban, NTP, kernel updates, cron, swap)
- Monitoring (auditor timers, reports rotation, disk-growth-forecast, unified daily health digest, ssh-key-reconciler, gemini-bridge, arena-chat, telegram-drift-guard, all-vectors-dev, monitoring dashboard)
- Documentation (SKILLS_INDEX, COMPACT_CONTEXT, runbook, node-topology, STATUS.md, arena-router-chat, AutoSklo deployment guide, backup/restore checklist)
- Final validation (all skills tests, SharedIntegrations tests, full endpoint sweep, parent↔ubu sync, all 5 auditors, all timers green, final session report, iteration log, experience document)

## Глобальные стоп-флаги

- SLO != green → остановить все потоки
- Disk >= 90% → остановить все потоки
- Failed systemd units > 0 для core-сервисов → остановить
- Parent restart без явного deploy окна → остановить
- Тесты core падают → остановить
- Операция требует удаление Garage/IPFS/CAS данных без live-read + off-host proof → остановить
- Внешний ресурс требует оплату/карту → остановить

## Рабочий ритм

- Wave size: 30-90 минут на поток, bounded
- Каждый поток пишет report в /root/agents/-Octopus/reports/
- Каждый поток обновляет experience при новом выводе/инциденте
- Commander каждые wave-end собирает launch_board
- Deploy только canary-first
- Все внешние side effects — dry-run по умолчанию
- Money/publication/PII/execution — approval gate

## График зависимостей

- Фаза 1 → Фаза 2 (безопасные next-waves)
- Фаза 2 → Фаза 3 (P1 foundation, ждёт completion Фазы 2)
- Фаза 3 → Фаза 4 (P2 product, ждёт completion Фазы 3)
- Фаза 5 → Финальная валидация (после completion всех фаз)