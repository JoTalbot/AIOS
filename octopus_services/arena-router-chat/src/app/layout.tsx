import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Arena Router — OpenAI-compatible proxy for arena.ai",
  description:
    "Send a standard /v1/chat/completions request and Arena Router drives arena.ai's web UI via headless browser automation to get a reply from any of 150+ models.",
  keywords: ["arena.ai", "lmarena", "openrouter", "openai", "proxy", "browser automation", "LLM"],
  authors: [{ name: "Arena Router" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
