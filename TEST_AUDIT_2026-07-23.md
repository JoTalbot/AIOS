# Аудит тестового набора — 23 июля 2026

## Среда и команда

- AIOS: `9.3.0` (версия из `pyproject.toml`)
- Python: `3.13.14`
- Команда: `python -m pytest -q`
- Результат: **1216 passed, 39 failed, 1 warning** за 118,24 с.

Это фактический локальный результат для текущего коммита `main`, а не
историческая метрика релиза.

## Группы сбоев

| Приоритет | Область | Количество | Наблюдение |
|---|---|---:|---|
| P0 | SQLite `Database` | 3 | Соединение используется из другого потока; транзакция не изолируется при повторном открытии БД. |
| P0 | `Telemetry` | 21 | Тесты ожидают публичные методы `create_counter`, `create_gauge`, `create_histogram`, экспорт и сброс метрик, которых в текущем классе нет. |
| P1 | Orchestrator | 5 | Несовместимость модели `Task` / `TaskStep` с контрактом базовых тестов. |
| P1 | Admin API export/import | 2 | E2E-экспорт и CSV-экспорт не проходят. |
| P1 | Нагрузочные тесты | 3 | Параллельное чтение БД, webhook-уведомления и burst-запросы. |
| P2 | Backup / API / validation | 5 | Верификация изменённого бэкапа, ожидание статуса malformed-запроса и ограничение размера экспорта. |

## Рекомендованный порядок исправления

1. Восстановить публичный API `Telemetry` и добавить регрессионные тесты.
2. Сделать SQLite-доступ потокобезопасным и определить явную политику транзакций.
3. Согласовать контракт оркестратора с его тестами.
4. Исправить E2E, нагрузочные сценарии и проверки безопасности.
5. Снова запустить полный набор на Python 3.11, 3.12 и 3.13; только после этого обновлять зелёные бейджи и release notes.

## Примечание по зависимостям

Перед запуском в чистой среде следует установить зависимости:

```bash
python -m pip install -r requirements.txt
python -m pytest -q
```

## Повторный прогон после восстановления Telemetry

После восстановления совместимого публичного API `Telemetry` выполнен повторный
прогон той же командой: **1238 passed, 17 failed, 1 warning** за 112,09 с.

Устранена вся группа из 21 падения `Telemetry` и ещё одно связанное
интеграционное падение. Оставшиеся задачи: SQLite/транзакции (3),
export/import (2), нагрузочные сценарии (3), backup/API/validation (4) и
совместимость оркестратора (5).
