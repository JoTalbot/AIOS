"""AIOS Phone Brain — умный демон-оркестратор Android-устройств.

Единый процесс вместо разрозненных systemd-таймеров и subprocess-вызовов:

* SQLite-очередь задач с арендой (lease), повторами и backoff;
* DeviceSupervisor — умный reconnect с экспоненциальным backoff и эскалацией;
* локальный HTTP API (127.0.0.1:8790) для постепенной миграции Telegram-бота;
* журнал событий — основа будущего reaction engine.

Этап 1 «умной архитектуры Android-шлюза». Поведенческие гейты безопасности
существующего шлюза (confirm-флаги, маскирование) сохранены как middleware.
"""

__version__ = "0.1.0"
