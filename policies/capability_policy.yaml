version: "2.1.1"

capability_policy:
  capability_levels:
    restricted:
      approval_required: true

    standard:
      audit_required: true

    trusted:
      expanded_access: true

  rules:
    least_privilege: true
    capability_review_required: true
    dangerous_actions_require_approval: true
