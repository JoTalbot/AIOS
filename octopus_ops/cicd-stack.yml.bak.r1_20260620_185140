version: "3.8"
services:
  github_runner:
    # Using a generic alpine image as a placeholder for the github runner
    image: alpine:latest
    command: ["tail", "-f", "/dev/null"]
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
      restart_policy:
        condition: any
