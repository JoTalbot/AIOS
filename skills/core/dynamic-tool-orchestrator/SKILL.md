---
name: dynamic-tool-orchestrator
description: "Центральный оркестратор динамического распределения инструментов и функционала между нодами. Решает что, где, когда запускать/останавливать на основе нагрузки, необходимости и целесообразности. Приоритет: ЖИТЬ + УПРОЩЕНИЕ."
---
# Dynamic Tool Orchestrator

## Описание

Этот скил предназначен для...

## Основная задача
Динамически управлять набором запущенных скриптов, сервисов, моделей и функционала на каждой ноде (и глобально) в зависимости от текущих нужд проекта/системы.

## Когда активировать
- При смене режима работы (разработка / production / maintenance / low-load)
- При изменении нагрузки на нодах
- При добавлении/удалении новых скиллов или инструментов
- По запросу "development mode" или "minimal mode"

## Workflow
1. Получить текущий demand (от resource-demand-evaluator)
2. Получить capabilities всех нод (capability-registry)
3. Получить текущую нагрузку (load-aware-scheduler)
4. Принять решение:
   - Какие инструменты/скрипты нужно запустить
   - На каких нодах
   - Какие остановить/удалить
5. Через script-deployer выполнить install/start/stop/uninstall (с consent)
6. Зарегистрировать изменения в capability-registry
7. Уведомить dynamic-capability-sync для распространения по федерации

## Режимы работы (примеры)
- **development**: выключить все тяжёлые модели, whisper, ollama, vector search если не нужны
- **production**: включить нужные сервисы на достаточном количестве нод
- **minimal**: оставить только core (loader, health, MCP stub)
- **high-load**: распределить нагрузку максимально равномерно

## Интеграция
- consent-orchestrator (обязательное согласие на изменение запущенных процессов)
- free-tier-orchestrator (учитывать стоимость/ресурсы)
- self-healing-swarm (при падении ноды перераспределять)
- load-aware-scheduler
- script-deployer

## Пример использования
"Переключи все ноды в development mode"
"Запусти voice-rag только на 2 нодах с наименьшей нагрузкой"
"Останови все модели на этой ноде"

## Алгоритм
1. Загрузить `SKILL.md`, контекст проекта Octopus и последние отчёты по направлению навыка.
2. Классифицировать навык по тегам (health/api/memory/disk/telegram/systemd/docker/security/ai).
3. Выполнить только безопасные read-only проверки через `code/run.py` и общий `generic_skill_runtime`.
4. Сформировать JSON-отчёт: статус, найденные факты, риски, рекомендации, следующий bounded-шаг.
5. Если требуется изменение системы — записать proposal/rollback в logs/reports и ждать consent gate либо выполнения автономным агентом в bounded-режиме.
6. Для Telegram: прямые push-уведомления запрещены, кроме `skill-notification` и отчётов автономного агента.
7. Для AWS/платных ресурсов: только аудит; создание/включение ресурсов запрещено без явной команды человека.

## Контроль и развитие
- Runtime: `code/run.py --json`.
- Contract tests: `tests/test_contract.py`.
- Мониторинг: `scripts/skill_evolution_cycle.py` пересчитывает health/coverage и дописывает AI-предложения в `references/`.
- Развитие через ИИ: локальный Ollama/Qwen генерирует bounded improvement proposal; автоприменяются только безопасные структурные улучшения (алгоритм, тест, runtime wrapper).
- Описание назначения: Центральный оркестратор динамического распределения инструментов и функционала между нодами. Решает что, где, когда запускать/останавливать на основе нагрузки, необходимости и целесообразности. Приоритет: ЖИТЬ + УПРОЩЕНИЕ.
