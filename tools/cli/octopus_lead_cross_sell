#!/usr/bin/env python3
import sys, json
sys.path.insert(0, '/mnt/agents/-SharedIntegrations/lead-pipeline')
from cross_sell_engine import evaluate_lead
lead = {'id': 'cli-' + sys.argv[1] if len(sys.argv) > 1 else 'cli-1', 'source_project': sys.argv[1] if len(sys.argv) > 1 else 'AutoHelp', 'intent': ' '.join(sys.argv[2:]) if len(sys.argv) > 2 else 'general'}
print(json.dumps(evaluate_lead(lead), ensure_ascii=False, indent=2))
