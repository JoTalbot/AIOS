#!/usr/bin/env python3
import json
from datetime import datetime, timezone
from pathlib import Path
B=Path('/root/agents/-Octopus/skills/core/money-earner-orchestrator'); D=B/'data'; OUT=D/'daily_summary_latest.json'
def load(path,d):
 try:return json.loads(Path(path).read_text())
 except Exception:return d
m=load(D/'metrics_snapshot_latest.json',{}); t=load(D/'timer_health_latest.json',{}); a=load(D/'roadmap_audit_latest.json',{}); o=load(D/'opportunity_aggregate_latest.json',{}); cfg=load(B/'config/faucet_config.json',{}); ss=load(D/'self_sufficiency_status_latest.json',{})
cap=cfg.get('captcha',{})
out={'generated_at':datetime.now(timezone.utc).isoformat(),'date':datetime.now(timezone.utc).date().isoformat(),'system':{'disk_used_percent':m.get('disk_used_percent'),'ram_used_percent':m.get('ram_used_percent'),'timers_healthy':t.get('healthy')},'finance':{'captcha_spent_usd':m.get('captcha_spent_usd'),'paper_realized_pnl_usd':m.get('paper_realized_pnl_usd'),'paid_captcha_blocked':not bool(cap.get('auto_paid_enabled',False)),'paid_captcha_user_override':bool(cap.get('auto_paid_enabled',False)),'captcha_daily_limit_usd':cap.get('max_daily_budget_usd'),'captcha_max_per_solve_usd':cap.get('max_cost_per_solve_usd')},'opportunities':{'live_verified_total':o.get('live_verified_total',0)},'self_sufficiency':{'level':ss.get('current_level'),'phase_status':ss.get('phase_status',{}),'blockers':ss.get('blockers',[])},'audit':{'errors':len(a.get('errors',[])),'future_timestamps':a.get('results',{}).get('accounting',{}).get('future_timestamps',[])}}
OUT.write_text(json.dumps(out,ensure_ascii=False,indent=2)+'\n'); print(json.dumps(out,ensure_ascii=False))
